<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <a class="navbar-brand" href="{{ path('home') }}">The Simple Bar/beers</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
            <li class="nav-item">
                <a class="nav-link  {% if route_name is defined and route_name == 'home' %} active {% endif %}" href="{{ path('home') }}">Home <span class="sr-only">(current)</span></a>
            </li>
            {% for category in categories %}
                <li class="nav-item">
                    <a class="nav-link {% if route_name is defined and route_name == 'category' and categoryId == category.id %} active {% endif %}" href="{{ path('category', { id: category.id }) }}">{{ category.name | capitalize }}</a>
                </li>
            {% endfor %}
            <li class="nav-item">
                <a class="nav-link {% if route_name is defined and route_name == 'mentions' %} active {% endif %}" href="{{ path('mentions') }}">Mentions</a>
            </li>
            <li class="nav-item">
                <a class="nav-link {% if route_name is defined and route_name == 'statistic' %} active {% endif %}" href="{{ path('statistic') }}">Statistic</a>
            </li>
            <li class="nav-item">
                <a class="nav-link {% if route_name is defined and route_name == 'consommation' %} active {% endif %}" href="{{ path('consommation') }}">Consommation</a>
            </li>
        </ul>
    </div>
</nav>